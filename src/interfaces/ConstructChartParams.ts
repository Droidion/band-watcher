// Chart input parameters when generating new chart
export default interface ConstructChartParams {
  elId: string;
  mac: string;
  chartColor: string;
  dataCategory: string;
  dataParam: string;
}
